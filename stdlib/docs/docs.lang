// Not going to work because of
// - unary operator priority 
// - Force/Quote operation under construction
// - bad binary operators parser?

docs #print = "Usual helloworld function"
docs #printenv = "Looks like it is used for printing environment"
docs #include = "Opens file and returns it as code"
docs #docs = "docs"

docs #Void = "Represents an instance of nothing"
docs #Any = "A set that contains everything including itself"

docs #(:) = "This is just an application operator, function is at right and the argument is at left"
docs #($) = "Low priority application operator"
docs #(=>) = "Then. Returns right operand only if left operand is true"

